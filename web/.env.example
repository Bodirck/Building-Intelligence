# Frontend env (Vite). Copy to web/.env to override the defaults.

# The app ships in mock mode (no backend needed). Set to "false" to call the real
# FastAPI backend through the dev proxy.
VITE_USE_MOCK=false

# Backend target for the /api dev proxy (default http://localhost:8000).
# VITE_API_TARGET=http://localhost:8000
